-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS `jay` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE `jay`;

-- Show success message
SELECT 'Database "jay" created successfully!' as Message;
